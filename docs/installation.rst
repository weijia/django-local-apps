============
Installation
============

At the command line::

    $ easy_install django-local-apps

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-local-apps
    $ pip install django-local-apps
